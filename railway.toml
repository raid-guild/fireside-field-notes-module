[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm build:clean"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"